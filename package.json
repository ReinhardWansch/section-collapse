{
  "name": "obsidian-section-collapse",
  "version": "0.0.0",
  "type": "module",
  "description": "A lightweight Obsidian plugin to collapse all sections in the active document with a single click, enhancing workspace organization and usability.",
  "main": "src/main.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "version": "node scripts/update-version.js"
  },
  "author": "Reinhard Wansch",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.0.10",
    "esbuild": "^0.25.5",
    "typescript": "^5.8.3"
  }
}
